<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for SpendTracker Pro
  Enforces HTTPS-only connections to the backend.
  Add your server's domain in base-config or domain-config.
-->
<network-security-config>

    <!-- PRODUCTION: Only trust system CAs (i.e. valid HTTPS cert required) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!--
        DEVELOPMENT ONLY: Allow cleartext (HTTP) to Android emulator localhost.
        Comment this out before publishing to Play Store.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>

</network-security-config>
